import styled from "styled-components"
import AppleIcon from '@material-ui/icons/Apple'

export const Container = styled.div`
    height: 80px;
    background-color: var(--primary);
`

export const Icon = styled(AppleIcon)`
    margin: 0 10px 0 0;
    font-size: 3em;
    color: white;
`